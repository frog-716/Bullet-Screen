## Agent skills

### 工单系统

工单与规格以本地 Markdown 文件存放在 `docs/tickets/`。具体约定见 `docs/agents/issue-tracker.md`。

### 分诊标签

使用默认的五类分诊标签。标签映射见 `docs/agents/triage-labels.md`。

### 领域文档

使用 `docs/domain/` 下的单上下文领域文档布局。读取规则见 `docs/agents/domain.md`。
